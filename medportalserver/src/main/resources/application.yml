spring:
    application:
        name: medportalserver

    datasource:
        url: jdbc:postgresql://localhost:5433/postgres
        username: admin
        password: admin123

    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                "[dialect]": org.hibernate.dialect.PostgreSQLDialect
                "[format_sql]": true
        show-sql: true
